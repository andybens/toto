{% extends '::base.html.twig' %}

{% block body %}
  {% include'::navbar.html.twig'  %}
   <div class="container">
     <h3>View single post</h3>
     <div class="list-group">
         <a href="#" class="list-group-item  list-group-item-action active">
           POST
         </a>
         <p class="list-group-item">{{post.title}}</p>
         <p class="list-group-item">{{post.description}}</p>
         <p class="list-group-item"> {{post.category}}</p>
        <p class="list-group-item"> <a href="{{ absolute_url(asset('')) }}app_dev.php/services" class="btn btn-primary btn-sm">Back</a></p>
      </div>
   </div>
   {% include'::footer.html.twig' %}
{% endblock %}
